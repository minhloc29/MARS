# Runtime dependencies for MARS and the integrated CVRP baselines.
# PyTorch, TorchRL, and TensorDict are installed separately by install_server.sh
# because their versions and wheel index must be compatible with each other.
einops>=0.8
hydra-core>=1.3
hydra-colorlog>=1.2
lightning>=2.1
matplotlib>=3.8
omegaconf>=2.3
pandas>=2.2
pyrootutils>=1.0
rich>=13
robust-downloader>=0.0.2
scipy>=1.12
wandb>=0.19
